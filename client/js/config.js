/**
 * 遊戲設定
 */
const CONFIG = {
    MASTER_URL: "http://localhost:8080/list",
    INPUT_RATE: 50,  // 輸入發送頻率 (ms)
    INTERPOLATION_FACTOR: 0.2,
    CAMERA_SMOOTHING: 0.1,
};

export default CONFIG;
