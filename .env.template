# ============================================
# Agar.io Clone - 環境變數設定範例
# 複製此檔案為 .env 並修改設定值
# ============================================

# === Game Server 設定 ===
SERVER_NAME=Agar.io AI Lab (Auto-Scale)
SERVER_HOST=localhost
SERVER_PORT=8765
MAX_PLAYERS=50

# === Master Server 設定 ===
MASTER_HOST=localhost
MASTER_PORT=8080
# 完整 URL (可選，會自動從 HOST:PORT 組合)
# MASTER_URL=http://localhost:8080

# === Health Check 設定 ===
# 健康檢查間隔 (秒)
HEALTH_CHECK_INTERVAL=30
# 離線超時時間 (秒)，超過則從列表移除
OFFLINE_TIMEOUT=300

# === Debug 設定 ===
DEBUG=false
